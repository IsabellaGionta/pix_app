import React from 'react';
import { View } from 'react-native'
import Card from '@paraboly/react-native-card'
function pracScreen({props, overlayid ,navigation, onPress}) {
    return (
        <Card
        iconDisable
        title="Title"
        description="Main Content"
        bottomRightText="30"
        onPress={() => {}}
      />
    );
}

export default pracScreen;